import 'package:flutter/material.dart';

/// Centralized color definitions for the app.
class AppColors {
  AppColors._();

  // Primary action / accent color
  static const Color primary = Color(0xFFFF4DA6);

  // Background gradient for login/home screens
  static const Color bgGradientStart = Color(0xFFFDEFF6);
  static const Color bgGradientEnd = Color(0xFFF8E9F2);

  // Muted text (approx. Colors.black54)
  static const Color mutedText = Color(0x8A000000);

  // White for on-primary text
  static const Color white = Color(0xFFFFFFFF);
}
